namespace DSA.NeetCode150.Topics.T02_TwoPointers.P05_TrappingRainWater_LC42
{
    public class TrappingRainWaterMySolution
    {
        // [0,2,0,3,1,0,1,3,2,1]

        // 1. Ignores zeros - left right
        // 1.2 Ignore elavation from corner
        // 

        /* first case:
            2 0 3 -> 2 0 2 -> 1 * 2 = 2;
        */

        /* second case:
            3 1 0 1 3
            
        */
        public int Trap(int[] height)
        {
            int l = 0, r = 0, area = 0, i = 0;

            while (i < height.Length)
            {
                if ()
                {
                    i++;
                } else
                {

                }
            }

            while (i < height.Length)
            {
                if (i > 0 && )
                {
                    l = height[i];
                }
                l = height[i];

                i++;
            }
        }
    }
}
